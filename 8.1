fhand = open(input('Enter filename: ')
lst = []
for line in fhand:
  line = lline.rstrip()
  words = line.split()
  for word in words:
    of word not in lst:
      lst.append(word)
lst.sort()
print (lst)
